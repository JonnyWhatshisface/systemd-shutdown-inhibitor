# Drop-in configuration for notifyusers script
# Disabled by default. Enable by setting enabled = true

[notifyusers]
command = /opt/terminusd/scripts/example-shutdown-notify.sh
priority = 100
user = root
group = root
env = /etc/inhibitor-env.d/notifyusers.env
enabled = false
