name: Unit Test - DBus Test Mode

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  test-dbus-test-mode:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libsystemd-dev

      - name: Build test binaries
        run: make test-build

      - name: Run DBus test-mode unit test
        run: make test-test-mode
