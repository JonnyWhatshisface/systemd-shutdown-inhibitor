# Drop-in configuration for applyupdates script
# Disabled by default. Enable by setting enabled = true

[applyupdates]
command = /usr/libexec/terminusd/examples/example-package-updates.sh
priority = 1000
critical = true
user = root
group = wheel
enabled = false
