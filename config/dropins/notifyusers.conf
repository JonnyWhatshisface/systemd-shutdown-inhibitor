# Drop-in configuration for notifyusers script
# Disabled by default. Enable by setting enabled = true

[notifyusers]
command = /usr/libexec/terminusd/examples/example-shutdown-notify.sh
priority = 100
user = root
group = root
enabled = false
